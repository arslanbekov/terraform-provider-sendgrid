---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sendgrid_domain_authentication_validation Resource - SendGrid provider"
subcategory: ""
description: |-
  
---

# sendgrid_domain_authentication_validation (Resource)



## Example Usage

```terraform
# Basic domain authentication setup
resource "sendgrid_domain_authentication" "main" {
  domain             = "mycompany.com"
  subdomain          = "em"
  is_default         = true
  automatic_security = true
  custom_spf         = false
}

# Check domain authentication validation
resource "sendgrid_domain_authentication_validation" "this" {
  domain_authentication_id = sendgrid_domain_authentication.main.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain_authentication_id` (String) Id of the domain authentication to validate.

### Optional

- `sub_user_on_behalf_of` (String) The subuser username for on-behalf-of API calls.

### Read-Only

- `id` (String) The ID of this resource.
- `valid` (Boolean) Indicates if this is a valid authenticated domain or not.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/bash

# Import domain authentication validation using its ID
terraform import sendgrid_domain_authentication_validation.this 12345
```
